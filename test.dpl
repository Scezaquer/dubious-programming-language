#include <std>

fn main(): int {
	let i: int = 12345678910;

	std::io::printcharln('h');
	std::io::printiln(i);

	let arr[3]: array[int] = [4, 5, 6];
	std::io::printarrayln:<int>(arr);

	let a: float = 3.14159265;

	let f: int = std::math::factorial(5);
	std::io::printiln(f);

	std::io::printboolln(0 == 0);

	return 0;
}