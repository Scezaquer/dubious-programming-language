struct S {
	first_attribute: int;
	second_attribute: char;
}

fn main(): int {
	let a : S = S{ 1, 'a' };
	let b : S = S{ 2, 'b' };
	let c : S = S{ 3, 'c' };
	let arr: array[S] = [a, b, c];
	return arr[1].second_attribute : int;
}